@model moment3cdsamling.Models.Artist

@{
    ViewData["Title"] = "Radera " + Model.Name;
}

<h1>@ViewData["Title"]</h1>

<h2>Är du säker på att du vill radera?</h2>
<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-3">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-9">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class = "col-sm-3">
            @Html.DisplayNameFor(model => model.Information)
        </dt>
        <dd class = "col-sm-9">
            @Html.DisplayFor(model => model.Information)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="ArtistId" />
        <input type="submit" value="Radera" class="btn btn-danger" />
        <a class="btn btn-primary" asp-action="Index">Tillbaka till listan</a>
    </form>
</div>
